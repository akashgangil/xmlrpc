Basic MultiRPC implementation by modifying the xmlrpc library (http://xmlrpc-c.sourceforge.net/)


INTRUCTIONS:

Build the source:
make clean
make

Run the servers:
./bin/service start 3

Run the clients:
./bin/client 1000

Stop the servers:
./bin/service stop
